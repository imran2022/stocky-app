/**
 * Purchase document vocabularies — statut enum → the i18n keys legacy renders
 * (payment statuses are shared with sales via pages/sales/saleVocab.js).
 */

export const PURCHASE_STATUSES = [
    { value: 'received', key: 'Received' },
    { value: 'pending', key: 'Pending' },
    { value: 'ordered', key: 'Ordered' },
];
